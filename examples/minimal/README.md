# Thatcher Minimal Example

# 1. Run the example
#    cd examples/minimal
#    bun x thatcher start

# 2. Open http://localhost:3000
#    - API: GET/POST/PUT/DELETE /api/item
#    - Workflow transitions: POST /api/item/:id/transition

# 3. Try the REPL
#    bun x thatcher console
#    > thatcher.list('item')
#    > thatcher.create('item', {name: 'Test'}, {id: 'system'})
